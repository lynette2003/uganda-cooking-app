services:
  - type: web
    name: ugandan-cooking-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: OPENAI_API_KEY
        fromDatabase: true  # or set manually in Render dashboard
    autoDeploy: true
    plan: free
